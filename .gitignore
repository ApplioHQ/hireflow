# KV data / backups contain user emails + password hashes — never commit them.
backups/
.wrangler/
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Secrets / credentials
.dev.vars
.env
.env.*
*.pem
*.key
secrets.*

# Dependencies / tooling
node_modules/
*.log
.DS_Store
hireflow-main.zip
